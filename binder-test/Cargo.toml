[package]
name = "binder-test"
version = "0.1.0"
edition = "2024"

[dependencies]
libbinder = { version = "0.1.0", path = "../libbinder" }
libbinder-raw = { version = "0.1.0", path = "../libbinder-raw" }
nix = "0.30.1"
process-sync = "0.2.2"
